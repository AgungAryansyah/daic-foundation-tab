model:
  name: tabiclv2
  checkpoint_version: tabicl-classifier-v2-20260212.ckpt
  parameters:
    n_estimators: 8
    batch_size: 8
    kv_cache: false
    device: auto
    use_amp: auto
    offload_mode: auto
    random_state: 42
    auto_recover_oom: false
