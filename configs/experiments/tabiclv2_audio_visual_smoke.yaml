extends:
  - tabiclv2_audio_visual.yaml

experiment:
  name: tabiclv2_binary_audio_visual_smoke

model:
  parameters:
    n_estimators: 1

evaluation:
  repeated_holdout:
    enabled: false

bootstrap:
  enabled: false
