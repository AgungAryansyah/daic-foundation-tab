extends:
  - tabiclv2_audio_visual_complete_cohort.yaml

experiment:
  name: tabiclv2_binary_audio_visual_complete_cohort_smoke

model:
  parameters:
    n_estimators: 1

evaluation:
  repeated_holdout:
    enabled: false

bootstrap:
  enabled: false
